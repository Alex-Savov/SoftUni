function sumOdd(n){

    let sum = 0;
    let index = 1;

    for(i = 0; i < n; i++) {

        console.log(index);
        sum += index;
        index+=2;
    }

console.log(`Sum: ${sum}`);
}

sumOdd(5);
sumOdd(3);